# Runtime dependencies for the biomed_rag app + ingest pipeline.
#
# Pinned to the generation that was built clean in Docker and validated end to
# end on the `hf-space` deploy branch (build green, LangGraph agent builds, a
# real query returns a cited answer). Eval + test extras live in
# requirements-dev.txt so the runtime install stays lean and resolvable.
#
# This file installs cleanly in a fresh venv:
#     python -m venv .venv && .venv/bin/pip install -r requirements.txt

# --- App / UI ---
streamlit==1.55.0
plotly==6.6.0
pandas==2.3.3
numpy==1.26.4
matplotlib==3.10.8   # required by the dashboard's pandas .background_gradient()

# --- Retrieval ---
faiss-cpu==1.13.2
rank-bm25==0.2.2

# --- Config / IO ---
python-dotenv==1.2.2
PyYAML==6.0.3

# --- Ingest helpers ---
ftfy==6.3.1
tqdm==4.67.3

# --- LLM stack (mutually consistent generation; do not mix with the old
#     langchain-core 0.3.x pins, which conflict with langgraph 1.x) ---
openai==2.43.0
tiktoken==0.13.0
langchain-core==1.4.8
langchain-openai==1.3.3
langgraph==1.2.6
